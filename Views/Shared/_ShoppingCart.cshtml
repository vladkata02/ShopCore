<ul class="nav navbar-nav navbar-right">
    <li>
        @if (ViewData["CartCounter"] == null)
        {
            @Html.ActionLink("Cart", "ShoppingCart", "Shopping", new { id = "cartItem" })
        }
        else
        {
            @Html.ActionLink("Cart("+ ViewData["CartCounter"]+")", "ShoppingCart", "Shopping", new { id = "cartItem" })
        }

    </li>
</ul>
